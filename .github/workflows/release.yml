name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Create release archive
        run: |
          mkdir -p /tmp/resource-suggest
          rsync -a --exclude node_modules --exclude .git --exclude .github --exclude src --exclude .idea . /tmp/resource-suggest/
          cd /tmp
          zip -r ${{ github.workspace }}/resource-suggest.zip resource-suggest

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: resource-suggest.zip
          generate_release_notes: true
